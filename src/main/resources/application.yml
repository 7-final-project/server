spring:
  application:
    name: server

server:
  port: 19000

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:19000/eureka/
  instance:
    hostname: localhost